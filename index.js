const greet = function(name) {
    return `Hello, ${name}!`;
  };

  
  const square = num => num * num;

  const add = (a, b) => {
    console.log(`Adding ${a} and ${b}`);
    return a + b;
  };
  
  const divide = function() {
    return 2000 / 100;
  };
  